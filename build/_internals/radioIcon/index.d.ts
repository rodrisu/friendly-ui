import React from 'react';
export type RadioIconProps = Readonly<{
    isChecked?: boolean;
    isLoading?: boolean;
    isDisabled?: boolean;
}>;
export declare const RadioIcon: ({ isChecked, isLoading, isDisabled, }: RadioIconProps) => React.JSX.Element;
